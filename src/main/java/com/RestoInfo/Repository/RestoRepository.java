package com.RestoInfo.Repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.RestoInfo.Model.Resto;

public interface RestoRepository extends JpaRepository<Resto,Integer>{

}
